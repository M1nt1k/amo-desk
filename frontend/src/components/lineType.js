export default function LineType() {
    return (
        <select name='lineParam' className='option-parametr'>
            {}
            {/* {references.map((r) => (<p >{r}</p>))} */}
        </select>
    )
}